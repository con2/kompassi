doctype html
html
  head
    title {{ title|safe }} | {{ KOMPASSI_INSTALLATION_NAME }}
  body
    header
      h1 {{ title|safe }}
      p Tulostettu {{ now }}.
    article(style='font-size: 10pt')
      for member in members
        div(style='display: inline-block; width: 24em') {{ member.to_html_print|safe }}
          if not filter_active
            |  ({{ member.get_state_display }})
    footer
      p {{ members.count }} hlö
