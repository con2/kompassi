extends base.jade
block content
  h2 {{ event.name }}

  form(action='{% url "labour_signup_view" event.pk %}', method='POST', role='form')
    include labour_signup_forms.html
    p
      // XXX why is this extra p necessary to preserve spacing
    fieldset
      legend Lähetä ilmoittautuminen
      input.btn.btn-primary(type='submit', name='submit', value='Lähetä ilmoittautuminen')
      | 
      a.btn.btn-danger(href='{% url "core_event_view" event.pk %}') Peruuta
      p
        // XXX why is this extra p necessary to preserve spacing

