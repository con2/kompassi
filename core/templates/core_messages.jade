.messages
  include core_language_warning.jade

  for message in messages
    if message.tags
      .alert(class="alert-{{ message.tags }}") {{ message|safe }}
    else
      .alert {{ message|safe }}
