h2 {{ settings.KOMPASSI_INSTALLATION_NAME_PARTITIVE }} käyttävät yhdistykset
for organizations_row in organizations_rows
  .row
    for organization in organizations_row
      .col-md-3
        .panel.panel-default
          .panel-heading
            strong
              a(href='{% url "core_organization_view" organization.slug %}')= organization.name
          .panel-body
            p= organization.muncipality
            if organization.membership_organization_meta
              if organization.receiving_applications
                .label.label-primary Jäsenrekisteri
              else
                .label.label-default Jäsenrekisteri
